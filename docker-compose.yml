version: "3.9"

x-service-base: &service-base
  build:
    context: .
    dockerfile: Dockerfile.services
  depends_on:
    postgres:
      condition: service_healthy
    migrations:
      condition: service_completed_successfully

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-hrms}
      POSTGRES_USER: ${POSTGRES_USER:-hrms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-hrms_dev_password}
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./deployment/config/postgres-init.sql:/docker-entrypoint-initdb.d/001-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-hrms} -d ${POSTGRES_DB:-hrms}"]
      interval: 10s
      timeout: 5s
      retries: 5

  migrations:
    image: postgres:16-alpine
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD:-hrms_dev_password}
      POSTGRES_USER: ${POSTGRES_USER:-hrms}
      POSTGRES_DB: ${POSTGRES_DB:-hrms}
      POSTGRES_HOST: postgres
    volumes:
      - ./deployment/migrations:/migrations:ro
      - ./deployment/scripts/run-migrations.sh:/run-migrations.sh:ro
    command: ["sh", "/run-migrations.sh"]
    restart: "no"

  employee-service:
    <<: *service-base
    environment:
      SERVICE_NAME: employee-service
      PORT: 8001
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
      AUTH_SERVICE_URL: http://auth-service:8006
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  attendance-service:
    <<: *service-base
    environment:
      SERVICE_NAME: attendance-service
      PORT: 8002
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
      EMPLOYEE_SERVICE_URL: http://employee-service:8001
      AUTH_SERVICE_URL: http://auth-service:8006
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8002/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  leave-service:
    <<: *service-base
    environment:
      SERVICE_NAME: leave-service
      PORT: 8003
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
      EMPLOYEE_SERVICE_URL: http://employee-service:8001
      AUTH_SERVICE_URL: http://auth-service:8006
      PAYROLL_SERVICE_URL: http://payroll-service:8004
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8003/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  payroll-service:
    <<: *service-base
    environment:
      SERVICE_NAME: payroll-service
      PORT: 8004
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
      EMPLOYEE_SERVICE_URL: http://employee-service:8001
      ATTENDANCE_SERVICE_URL: http://attendance-service:8002
      LEAVE_SERVICE_URL: http://leave-service:8003
      AUTH_SERVICE_URL: http://auth-service:8006
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8004/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  hiring-service:
    <<: *service-base
    environment:
      SERVICE_NAME: hiring-service
      PORT: 8005
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
      EMPLOYEE_SERVICE_URL: http://employee-service:8001
      AUTH_SERVICE_URL: http://auth-service:8006
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8005/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  auth-service:
    <<: *service-base
    environment:
      SERVICE_NAME: auth-service
      PORT: 8006
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
      JWT_ISSUER: ${JWT_ISSUER:-sme-hrms}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-sme-hrms-api}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8006/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  notification-service:
    <<: *service-base
    environment:
      SERVICE_NAME: notification-service
      PORT: 8007
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
      AUTH_SERVICE_URL: http://auth-service:8006
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8007/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  audit-service:
    <<: *service-base
    environment:
      SERVICE_NAME: audit-service
      PORT: 8008
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8008/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  workflow-service:
    <<: *service-base
    environment:
      SERVICE_NAME: workflow-service
      PORT: 8009
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8009/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  performance-service:
    <<: *service-base
    environment:
      SERVICE_NAME: performance-service
      PORT: 8010
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8010/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  engagement-service:
    <<: *service-base
    environment:
      SERVICE_NAME: engagement-service
      PORT: 8011
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8011/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  helpdesk-service:
    <<: *service-base
    environment:
      SERVICE_NAME: helpdesk-service
      PORT: 8012
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8012/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  reporting-analytics-service:
    <<: *service-base
    environment:
      SERVICE_NAME: reporting-analytics-service
      PORT: 8013
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8013/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  search-service:
    <<: *service-base
    environment:
      SERVICE_NAME: search-service
      PORT: 8014
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8014/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  expense-service:
    <<: *service-base
    environment:
      SERVICE_NAME: expense-service
      PORT: 8015
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8015/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  integration-service:
    <<: *service-base
    environment:
      SERVICE_NAME: integration-service
      PORT: 8016
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8016/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  automation-service:
    <<: *service-base
    environment:
      SERVICE_NAME: automation-service
      PORT: 8017
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8017/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  travel-service:
    <<: *service-base
    environment:
      SERVICE_NAME: travel-service
      PORT: 8018
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8018/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  project-service:
    <<: *service-base
    environment:
      SERVICE_NAME: project-service
      PORT: 8019
      DATABASE_URL: postgresql://${POSTGRES_USER:-hrms}:${POSTGRES_PASSWORD:-hrms_dev_password}@postgres:5432/${POSTGRES_DB:-hrms}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8019/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile.services
    command: ["python", "/app/docker/api_gateway_service.py"]
    ports:
      - "8000:8000"
    environment:
      PORT: 8000
      EMPLOYEE_SERVICE_URL: http://employee-service:8001
      ATTENDANCE_SERVICE_URL: http://attendance-service:8002
      LEAVE_SERVICE_URL: http://leave-service:8003
      PAYROLL_SERVICE_URL: http://payroll-service:8004
      HIRING_SERVICE_URL: http://hiring-service:8005
      AUTH_SERVICE_URL: http://auth-service:8006
      NOTIFICATION_SERVICE_URL: http://notification-service:8007
      AUDIT_SERVICE_URL: http://audit-service:8008
      WORKFLOW_SERVICE_URL: http://workflow-service:8009
      PERFORMANCE_SERVICE_URL: http://performance-service:8010
      ENGAGEMENT_SERVICE_URL: http://engagement-service:8011
      HELPDESK_SERVICE_URL: http://helpdesk-service:8012
      REPORTING_ANALYTICS_SERVICE_URL: http://reporting-analytics-service:8013
      SEARCH_SERVICE_URL: http://search-service:8014
      EXPENSE_SERVICE_URL: http://expense-service:8015
      INTEGRATION_SERVICE_URL: http://integration-service:8016
      AUTOMATION_SERVICE_URL: http://automation-service:8017
      TRAVEL_SERVICE_URL: http://travel-service:8018
      PROJECT_SERVICE_URL: http://project-service:8019
    depends_on:
      employee-service:
        condition: service_healthy
      attendance-service:
        condition: service_healthy
      leave-service:
        condition: service_healthy
      payroll-service:
        condition: service_healthy
      hiring-service:
        condition: service_healthy
      auth-service:
        condition: service_healthy
      notification-service:
        condition: service_healthy
      audit-service:
        condition: service_healthy
      workflow-service:
        condition: service_healthy
      performance-service:
        condition: service_healthy
      engagement-service:
        condition: service_healthy
      helpdesk-service:
        condition: service_healthy
      reporting-analytics-service:
        condition: service_healthy
      search-service:
        condition: service_healthy
      expense-service:
        condition: service_healthy
      integration-service:
        condition: service_healthy
      automation-service:
        condition: service_healthy
      travel-service:
        condition: service_healthy
      project-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/ready')"]
      interval: 15s
      timeout: 5s
      retries: 5

  frontend-ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    ports:
      - "3000:80"
    depends_on:
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost/ >/dev/null 2>&1"]
      interval: 20s
      timeout: 5s
      retries: 5

volumes:
  pg_data:
