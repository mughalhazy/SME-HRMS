# Read Models

> **Canonical source:** All read model contracts, field-level schemas, and coverage checks are in `docs/canon/read-model-catalog.md`.

This file is retained as a pointer only. Search/indexing extensions (`document_library_view`, `global_search_view`) have been merged into the catalog: `document_library_view` is entry 14 and `global_search_view` is entry 25 (per `docs/canon/read-model-catalog.md`).

> NOTE (Group C fix, 2026-06-13): The original text read "entries 13 and 14" — stale. `document_library_view` is entry 14 and `global_search_view` is entry 25 in `read-model-catalog.md` (entry 13 is `integration_delivery_view`, unrelated to search).
